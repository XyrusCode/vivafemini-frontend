// Nitro server configuration for TanStack Start.
// Sets the deployment preset to 'vercel' so that `pnpm build` outputs
// to .vercel/output/ (Vercel Build Output API format) which Vercel
// natively understands without additional routing configuration.
// See: https://nitro.build/deploy/providers/vercel
export default {
  preset: 'vercel',
}
